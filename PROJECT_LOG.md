# ADERVIS Website — Журнал проекта

> Источник правды о статусе проекта между сессиями. Обновляется после каждого блока задач — перед чисткой диалога сюда выгружается актуальное состояние, и пушится на GitHub (`origin/main`, repo `adervisdigital-web/Adervis-website`).

## Контекст

Переносим adervis.ru на новый статический сайт (чистый HTML/CSS/JS, без фреймворков).
- Дизайн-система — `DESIGN.md` (Dark OLED Professional: DM Sans + Space Grotesk + JetBrains Mono, фиолетовый акцент `hsl(265,82%,58%)`, шкала отступов 4px).
- Компоненты `header.html` / `footer.html` подгружаются через `js/app.js` (`fetch` → `innerHTML`).
- Главная страница — обзорная/информационная (знакомство клиента с компанией). Страницы направлений (`/video`, `/design`, `/photo`, `/ai`) — продающие.
- Дополнительно продвигаем отдельный продукт — CRM **Adervis PRO** (app.adervis.ru) — должен быть на главной и на `/pro`.

## Структура

```
index.html            — главная (готова)
video/index.html      — заглушка, контент не перенесён
design/index.html     — заглушка
photo/index.html      — заглушка
ai/index.html         — заглушка
careers/index.html    — заглушка
pro/index.html        — заглушка
components/header.html, footer.html
css/style.css
js/app.js
DESIGN.md             — дизайн-система (источник правды по стилю)
```

## Статус по этапам

### ✅ Этап 1 — Скаффолдинг страниц (готово)
- Созданы все страницы-заглушки (video/design/photo/ai/careers/pro), каждая с шапкой/H1/футером.
- Создан `components/footer.html`, подключён через `app.js`.
- В навигацию шапки добавлена ссылка «Карьера» (её не было).
- Все ссылки в nav проверены — ведут на правильные пути.

### ✅ Этап 2 — Главная страница (готово)
Полностью собрана и визуально проверена (headless Edge скриншоты):
1. Hero — заголовок-обещание + CTA «Смотреть шоурил» / «Обсудить проект»
2. Цифры о работе — 700+ проектов / 350+ роликов / 100+ компаний / 60+ ниш
3. «Чем мы можем помочь» — 4 карточки направлений (Видео/Дизайн/Фото/ИИ) со ссылками на соответствующие разделы
4. «Нам доверяют» — блок доверия + строка клиентов (Anatomia, Аратта, Сибур, Химпром, Mobilov, Express-Import, Promobot)
5. Баннер **Adervis PRO / CRM** — бейдж + питч + кнопка на app.adervis.ru и `/pro`
6. Контактный CTA — телефон/email кликабельные (`tel:`, `mailto:`)
7. Футер — бренд, навигация, контакты, соцсети (Behance, VK, Instagram)

Также допилена дизайн-система: добавлены недостающие spacing-токены (`--sp-1…--sp-16`, `--primary2`) точно по `DESIGN.md`, поправлен `line-height` заголовков (1.5 → 1.15).

### ⏭ Этап 3 — Контент по направлениям (в работе)
Переносим контент с adervis.ru постранично. Порядок:
1. **`/video`** — следующий шаг (больше всего материала: кейсы, портфолио роликов, отзывы)
2. `/design`
3. `/photo`
4. `/ai`
5. `/careers`
6. `/pro` (дополнительно к CRM-баннеру на главной — отдельная посадочная страница продукта)

## Договорённости / решения

- Не клонировать сторонние Claude Code skill-репозитории в проект — они ставятся отдельно как плагины Claude Code (не код сайта). Из обсуждённого списка выбраны для установки как skills: **Anthropic Frontend Design** (`anthropics/claude-code` → плагин `frontend-design`) и **Interface Design** (`Dammyjay93/interface-design`). Оба репо проверены — есть валидный `.claude-plugin/marketplace.json`, команды установки отправлены пользователю (выполняются в его сессии Claude Code, т.к. меняют глобальный конфиг — `/plugin marketplace add ...` → `/plugin install ...`).
- Контакты: `+7 (922) 301-88-80`, `adervis.digital@gmail.com`. Соцсети: Behance (behance.net/adervisdigital), VK (vk.com/adervis_digital), Instagram (instagram.com/adervis_digital).
- Adervis PRO — отдельный продукт компании (CRM для продакшн-студий), живёт на `app.adervis.ru`, должен явно продвигаться с главной.

## Открытые задачи / TODO

- [ ] Установить skills для Claude Code: Anthropic Frontend Design, Interface Design (уточнить механизм установки в среде пользователя)
- [ ] Перенести контент `/video`
- [ ] Перенести контент `/design`, `/photo`, `/ai`, `/careers`, `/pro`
- [ ] Финальный аудит дизайна по `DESIGN.md` после переноса всего контента

---
*Обновляйте эту секцию датой и итогами в конце каждого блока задач.*
